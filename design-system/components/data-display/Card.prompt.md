A quiet surface container. Flat by default (hairline + 8px radius); `elevated` floats it for popovers/dialogs.

```jsx
<Card>Note preview…</Card>
<Card elevated padding={20}>Command palette</Card>
```
